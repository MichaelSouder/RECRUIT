# Builds/pulls all images needed for production and uploads them as a downloadable artifact.
# Does not store large binaries in git — download from the workflow run’s Artifacts section.

name: Export container images

on:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    env:
      IMAGE_TAG: latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set IMAGE_PREFIX (GHCR, lowercase)
        run: |
          p=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_PREFIX=ghcr.io/${p}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Export images to container-images (linux/amd64)
        run: |
          chmod +x scripts/export-container-images.sh
          ./scripts/export-container-images.sh

      - name: List exported files (verify Postgres + Redis + app images)
        run: |
          ls -la container-images/
          test -f container-images/postgres-15.tar
          test -f container-images/redis-7-alpine.tar
          test -f container-images/recruit-backend.tar
          test -f container-images/recruit-frontend.tar
          test -f container-images/AIRGAP_DEPLOY.md
          test -f container-images/airgap-cli
          test -x container-images/airgap-cli
          test -d container-images/airgap
          test -f container-images/airgap/cli.py
          test -f container-images/recruit-airgap.env.example

      # Split into two artifacts so Postgres/Redis are clearly visible and to reduce single-zip size limits.
      - name: Upload artifact — Postgres & Redis (infrastructure)
        uses: actions/upload-artifact@v4
        with:
          name: recruit-infra-postgres-redis
          path: |
            container-images/postgres-15.tar
            container-images/redis-7-alpine.tar
            container-images/README-AIR-GAP.txt
            container-images/AIRGAP_DEPLOY.md
            container-images/airgap-cli
            container-images/airgap/
            container-images/recruit-airgap.env.example
          if-no-files-found: error
          retention-days: 30

      - name: Upload artifact — RECRUIT backend & frontend
        uses: actions/upload-artifact@v4
        with:
          name: recruit-app-backend-frontend
          path: |
            container-images/recruit-backend.tar
            container-images/recruit-frontend.tar
            container-images/MANIFEST.txt
            container-images/README-AIR-GAP.txt
            container-images/AIRGAP_DEPLOY.md
            container-images/airgap-cli
            container-images/airgap/
            container-images/recruit-airgap.env.example
          if-no-files-found: error
          retention-days: 30
